

import 'package:flutter/material.dart';

class ArticleDetails extends StatelessWidget {
  const ArticleDetails({Key? key}) : super(key: key);
  static String  routeName="ArticleDetails";

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
